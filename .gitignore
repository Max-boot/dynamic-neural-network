# Python
__pycache__/
*.pyc

# MNIST-Rohdaten (werden beim Lauf automatisch geholt)
mnist/
data/

# Trainings-Logs / temporäre Dateien
*_log.txt
*_err.txt
*_pid.txt

# OS
.DS_Store
Thumbs.db

# LibreOffice / ODT-Lock-Dateien (nicht einchecken)
.~lock.*#?*
#__lock__.*

# Diagnose-/Fehler-Logs
blobio_err.txt
*_err.txt

# Lokale Entwicklungsumgebung (nicht auf GitHub)
.vscode/
.conda/

# Archiv (alte Experimente, lokale Bereinigung)
_archive/

# Datensätze / Assets (zu gross fürs Repo, werden lokal geladen/erzeugt)
cluttered_mnist/
corrupted_mnist/
dataset_build/
mnist/
data/
Samples/
scene_dataset/
scene_dataset_smoke/
scene_dataset_svhn/

# Git-Working-Copy-Altstand (nach .git-Umzug zu archivieren)
github-export/

# Trainings-Checkpoints (kommen nicht ins Repo, liegen nur lokal)
pipeline/models/*.pt
pipeline/models/*.pkl
models/*.pt

# Ergebnisse/Ausgaben
pipeline/results/
# Debug-/Diagnose-Skripte (Scratch, nicht im Repo)
pipeline/debug_*.py
